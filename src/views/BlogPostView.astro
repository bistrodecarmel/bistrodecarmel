---
import { render } from 'astro:content';
import type { CollectionEntry } from 'astro:content';
import BaseLayout from '../layouts/BaseLayout.astro';
import Header from '../components/Header.astro';
import Footer from '../components/Footer.astro';
import PageHero from '../components/PageHero.astro';
import CtaBand from '../components/CtaBand.astro';

interface Props {
  entry: CollectionEntry<'blog'>;
}

const { entry } = Astro.props;
const { Content } = await render(entry);
const isArticle = entry.data.kind === 'article';
---

<BaseLayout title={entry.data.metaTitle} description={entry.data.description}>
  <Header />

  <main>
    <PageHero
      eyebrow={isArticle ? 'הבלוג של ביסטרו דה כרמל' : 'ביסטרו דה כרמל'}
      title={`<strong>${entry.data.title}</strong>`}
    />

    <section class="page-section-tight">
      <div class="container">
        {
          isArticle && (
            <nav class="breadcrumbs" aria-label="פירורי לחם">
              <a href="/">דף הבית</a>
              <span aria-hidden="true">‹</span>
              <a href="/בלוג/">בלוג</a>
              <span aria-hidden="true">‹</span>
              <span>{entry.data.title}</span>
            </nav>
          )
        }
        <img
          class="ornament-divider post-ornament"
          src="/images/decor/divider-vine.webp"
          alt=""
          aria-hidden="true"
          loading="lazy"
        />
        <article class="prose post-body">
          <Content />
        </article>
      </div>
    </section>

    <div class="watercolor-band on-light" aria-hidden="true">
      <img src="/images/decor/vineyard-watercolor.jpg" alt="" loading="lazy" data-parallax="50" />
    </div>

    <CtaBand instanceId={`post-${entry.id}`} />
  </main>

  <Footer />
</BaseLayout>

<style>
  .breadcrumbs {
    display: flex;
    gap: 0.6rem;
    align-items: baseline;
    font-size: 0.85rem;
    font-weight: 500;
    color: var(--color-muted);
    margin-block-end: 2rem;
  }

  .breadcrumbs a {
    color: var(--color-bronze-text);
    text-decoration: none;
  }

  .breadcrumbs a:hover {
    text-decoration: underline;
  }

  /* מיושר לתחילת הטקסט (ימין ב-RTL), כמו הכותרות והפירורים מעליו */
  .post-ornament {
    margin-inline: 0 auto;
    margin-block-end: 2rem;
  }

  /* מדד קריאה צר יותר מ-.prose הכללי — מאמר שלם צריך שורה קצרה יותר לנוחות קריאה
     מפסקת שיווקית קצרה שיכולה לשאת את רוחב ה-760px התקני */
  .post-body {
    max-width: 660px;
  }

  /* מפריד עדין לפני כל כותרת משנה — שובר את גוש הטקסט הארוך בלי נכס תמונה נוסף */
  .post-body :global(h2) {
    font-size: 1.6rem;
    font-weight: 600;
    margin-block: 1.9em 0.5em;
    display: flex;
    align-items: center;
    gap: 0.7rem;
  }

  .post-body :global(h2::before) {
    content: '';
    display: inline-block;
    width: 6px;
    height: 6px;
    flex-shrink: 0;
    background: var(--color-bronze-line);
    transform: rotate(45deg);
  }

  .post-body :global(h2:first-child) {
    margin-block-start: 0;
  }

  .post-body :global(h3) {
    font-size: 1.25rem;
    margin-block: 1.4em 0.4em;
  }

  .post-body :global(a) {
    color: var(--color-bronze-text);
  }
</style>
